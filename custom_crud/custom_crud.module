<?php

function custom_crud_form_alter(&$form, &$form_state, $form_id) {
  if ($form_id == 'my_crudform_id') {
    $form['field_message'] = array(
      '#type' => 'item',
      '#title' => t('hook_form_alter testing'),
      '#markup' => t('hook_form_alter markup'),
      '#weight' => -6,
    );
  }
}